package Planes;

public class TemporaryPlane extends Plane {
    public TemporaryPlane(String model, int maxSpeed, int maxFlightDistance, int maxLoadCapacity) {
        super(model, maxSpeed, maxFlightDistance, maxLoadCapacity);
    }

    //TODO implement it later
}
